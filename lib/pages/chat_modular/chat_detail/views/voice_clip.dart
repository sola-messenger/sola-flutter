


import 'package:flutter/material.dart';

class VoiceClip extends StatelessWidget {
  final String avatar;
  final String content;
  final bool isOwen;

  const VoiceClip({super.key, required this.avatar, required this.content, required this.isOwen});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Container(),
    );
  }
}